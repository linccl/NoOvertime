# NoOvertime 需求文档

> 本文档用于沉淀需求与决策，随沟通滚动更新。任何未明确事项默认视为**未决**，需在“未决问题”中闭环。

## 1. 背景

- 项目代号：NoOvertime
- 当前状态：全新项目（仓库初始化阶段）
- 核心业务问题：在弹性工作制下，消除员工对“工时差值”的不确定性（何时下班才满 9 小时、当月到底可调休还是需补班）。

## 2. 目标（Goals）

- 提供一个用于记录上下班打卡时间、自动计算工时与调休/补班的移动端 App。
- 用户在设置的时间段内启动 App 时自动打卡（上班/下班分别触发不同逻辑）。
- 将“半小时计量（超出向下取整、不足向上取整）”规则自动化，实时量化调休资产（正为可调休、负为需补班）。
- App 侧完成所有计算并展示数据；支持同步到云数据库。
- 提供 Web 端只读数据看板（无打卡功能），从云数据库读取并展示统计结果。

## 3. 范围（Scope）

### 3.1 本期包含（In Scope）

- 移动端 App
  - 自动打卡：上班时间段触发上班打卡；下班时间段触发下班打卡
  - 手动打卡：支持手动打卡、补卡、修改打卡时间
  - 统计与展示：每日工时、月总工时、月可调休时长、月需补班时长
  - 请假管理：支持登记/修改/删除（上午半天/下午半天/全天）
  - 本地存储：所有数据先落手机本地
  - 云同步：每次生成/修改/删除打卡或请假记录即自动同步；也支持手动点击“同步”
  - 日期/月份切换：查看任意日期、任意月份的统计与列表
- Web 端（只读）
  - 从云数据库读取并展示：每日工时、月总工时、月可调休时长、月需补班时长
  - 支持日期/月份切换与列表浏览（无打卡、无计算）
- 云数据库
  - 存储移动端同步的原始记录与（或）计算结果，供 Web 端展示

### 3.2 本期不包含（Out of Scope）

- Web 端打卡
- 在未启动 App 的情况下后台自动打卡（仅在“启动 App”时触发）
- 跨月结转/抵扣调休（各月份独立计算，互不影响，详见业务规则）
- Web 端扫码配对（MVP 仅输入配对码）
- iOS 客户端（MVP 仅 Android）

## 4. 用户与场景

### 4.1 目标用户

- 个人用户：追求工作生活平衡（WLB）的弹性办公开发者/白领
  - 制度环境：公司实行弹性上下班，但要求每天满固定工时（如 9 小时）
  - 心理诉求：不想多干无谓加班，也不想因计算失误导致迟到/调休不准
  - 使用习惯：手机端负责打卡与计算；电脑端（Web）用于查看统计

### 4.2 核心使用场景（Top Scenarios）

- 场景 A：上班时间段内启动 App → 自动上班打卡 → 晚些时候下班时间段内启动 App → 自动下班打卡 → 生成当日工时与调休增减
- 场景 B：当月查看任意日期工时、当月累计工时与调休/补班余额
- 场景 C：登记请假（上午/下午/全天）→ 当天不累计也不消耗调休
- 场景 D：手机端同步到云 → Web 端查看同样的统计与列表
- 场景 E：手动打卡/补卡/修改打卡时间 → 重新计算当日与当月统计 → 同步到云端供 Web 查看

## 5. 需求拆解

> 建议我们先补齐“用户—场景—价值—验收”，再进入具体功能/页面/接口设计。

### 5.1 用户故事（User Stories）

- 作为【个人用户】，我希望【自动记录上下班打卡时间】，以便【不用手动记工时】。
- 作为【个人用户】，我希望【自动计算每日工时与当月累计】，以便【快速知道是否达标】。
- 作为【个人用户】，我希望【把超出/不足 9 小时的部分按规则换算为调休/补班】，以便【清楚当月可调休或需补班时长】。
- 作为【个人用户】，我希望【手机端数据可同步到云端、Web 端只读查看】，以便【在电脑上方便浏览统计】。

### 5.2 功能需求（Functional Requirements）

- FR-001：支持记录每日上班打卡时间（1 次）与下班打卡时间（1 次）。
- FR-002：支持配置“上班时间段”“下班时间段”；用户在对应时间段内启动 App 时自动触发对应打卡逻辑。
- FR-003：默认自动打卡时间段：上班 08:00-10:00；下班 17:00-19:00（均可配置）。
- FR-004：自动打卡上班与自动打卡下班在同一天内各自只触发一次（避免重复打卡）。
- FR-005：支持手动打卡（上班/下班）。
- FR-006：支持补卡：选择日期与时间补录上班/下班打卡。
- FR-007：支持修改已存在的打卡时间（上班/下班）。
- FR-008：下班打卡限制：当日缺少上班打卡时，禁止触发下班打卡（自动与手动均适用；可先补录上班再下班）。
- FR-009：统计计算前置条件：当日必须同时存在上班与下班打卡，才计算“当日工时”及对“当月调休/补班余额”的影响。
- FR-010：支持展示并计算每日工时（基于当日上/下班打卡时间）。
- FR-011：支持计算并展示当月月总工时（当月每日工时累计）。
- FR-012：支持计算并展示当月“可调休时长/需补班时长”（同一余额的正负展示）。
- FR-013：支持请假管理（登记/修改/删除）：上午半天（09:00-12:00）/下午半天（12:00-18:00）/全天。
- FR-014：请假当天：无论工作多久，均不可累计调休时长，也不可消耗调休时长（当日调休变动为 0）。
- FR-015：支持移动端本地存储。
- FR-016：每次生成/修改/删除一条打卡或请假记录即自动同步到云数据库；同时提供手动点击“同步”。
- FR-017：Web 端仅从云数据库读取并展示统计结果（无打卡、无计算）。
- FR-018：支持日期切换与月份切换查看数据。
- FR-019：提供“日期列表”视图：仅展示选中月份的数据（默认当月）。
- FR-020：提供“月份列表”视图：仅展示选中年份的 12 个月（默认当年）。
- FR-021：支持移动端生成“配对码”，用于 Web 端绑定只读查看。
- FR-022：支持重置配对码（重置后旧配对码失效）。
- FR-023：支持换机迁移：新手机输入配对码发起迁移请求，旧手机确认后新手机接管写入，旧手机强制退出。
- FR-024：配对码默认掩码显示；点按可显示明文。
- FR-025：配对码支持一键复制。
- FR-026：支持生成“恢复码”（用于旧机不可用时的强制迁移/接管），默认掩码显示；点按可显示明文；支持一键复制。
- FR-027：恢复码仅展示一次（首次生成或重置后）；若用户未保存，需要重置生成新恢复码（旧恢复码失效）。
- FR-028：支持强制迁移：旧机丢失/损坏/无法联网确认时，新手机使用“配对码 + 恢复码”接管写入；旧机后续上线需强制退出。
- FR-029：支持删除打卡记录（上班/下班）；删除后需重算当日与当月统计并同步。
- FR-030：当同一天已存在同类型打卡记录时，再次打卡/补卡需弹窗二次确认；确认后按“修改”覆盖原记录。
- FR-031：全天请假当日禁用自动打卡（仅允许手动打卡/补卡/修改/删除）。
- FR-032：打卡时间以“分钟”为最小粒度存储与计算（秒/毫秒必须为 0，即分钟精度）。
- FR-033：移动端需生成并同步“月度汇总（MonthSummary）”，Web 端直接读取展示月总工时与月可调休/需补班（不在 Web 端做计算）。

### 5.3 非功能需求（Non-functional Requirements）

- 性能：关键交互（首页加载、日期切换、月份切换、日期列表刷新、月份列表刷新）需在 2 秒内完成响应（不考虑网络延迟）。
- 可用性：离线可用（本地记录与计算）；联网后自动同步，支持手动同步重试。
- 安全：云端访问需鉴权（MVP 不引入独立账号体系，采用配对码绑定 Web 只读查看 + 恢复码迁移）；配对码/恢复码/迁移相关接口需限流与失败次数限制以避免暴力猜测；避免在日志/同步数据中泄露敏感信息。
- 兼容性：移动端仅 Android；Web 端桌面浏览器优先，移动端浏览器自适应非必需（可选）。

### 5.4 页面与信息架构（建议，MVP）

> 具体 UI 以实现阶段为准；此处用于对齐信息结构与交互闭环。

- 移动端
  - 首页/今日：展示今日打卡状态（缺上班/缺下班/已完成）、今日工时（若可算）、今日调休变动（若可算）、当月可调休/需补班
  - 打卡：手动上班/下班按钮；补卡入口（选择日期时间）；编辑入口（修改已打卡时间）；删除打卡记录
  - 日历/日期列表（按月）：列表展示当月每天的上班/下班、工时、调休变动与迟到标识；支持点进“日详情”
  - 月份列表（按年）：展示当年 12 个月的月总工时与月可调休/需补班
  - 请假：按日期做请假管理（登记/修改/删除；上午/下午/全天）
  - 设置：上班/下班自动打卡时间段；同步状态；配对码（生成/复制/明文切换/重置/换机迁移）；恢复码（生成/复制/明文切换/重置）；（可选）账号与登录
- Web 端（只读）
  - 配对：输入配对码绑定只读查看
  - 月看板：当月总览（工时/可调休/需补班）+ 日期列表
  - 年看板：当年月份列表（12 个月）

### 5.5 数据结构（建议，便于同步与展示）

> 仅定义领域模型与关键字段；具体表结构/索引以 `docs/数据库方案草案.md`（PostgreSQL）落地为准。

#### 5.5.1 云同步载荷白名单（强约束）

- `POST /api/v1/sync/commits` 请求 JSON 采用严格白名单：出现任意未知字段直接拒绝（`error_code=UNKNOWN_FIELD`），避免“客户端带了额外字段但服务端静默忽略”。详见 `docs/API契约草案.md` 与 `docs/同步上报实现基线-T001.md`。
- 因此：客户端本地模型可以有扩展字段，但同步上报必须严格按白名单字段构造（例如：不要在同步子对象里携带 `userId`、`createdAt/updatedAt`、`note` 等未定义字段）。

#### 5.5.2 字段命名映射（需求文档 camelCase ↔ API/DB snake_case）

| 需求文档（camelCase） | API/DB（snake_case） | 备注 |
|---|---|---|
| `userId` | `user_id` | `sync/commits` 顶层字段 |
| `deviceId` | `device_id` | `sync/commits` 顶层字段 |
| `writerEpoch` | `writer_epoch` | `sync/commits` 顶层字段 |
| `syncId` | `sync_id` | 幂等键之一 |
| `payloadHash` | `payload_hash` | 幂等键之一 |
| `localDate` | `local_date` | `YYYY-MM-DD` |
| `atUtc` | `at_utc` | UTC RFC3339（分钟精度，秒/毫秒为 0） |
| `timezoneId` | `timezone_id` | IANA 时区名 |
| `minuteOfDay` | `minute_of_day` | `0..1439` |
| `leaveType` | `leave_type` | `AM/PM/FULL_DAY` |
| `deletedAtUtc` | `deleted_at` | 软删除时间（UTC） |
| `startAtUtc` | `start_at_utc` | DaySummary |
| `endAtUtc` | `end_at_utc` | DaySummary |
| `isLeaveDay` | `is_leave_day` | DaySummary |
| `isLate` | `is_late` | DaySummary |
| `workMinutes` | `work_minutes` | DaySummary |
| `adjustMinutes` | `adjust_minutes` | DaySummary |
| `updatedAtUtc` | `updated_at` | Day/Month Summary |
| `monthStart` | `month_start` | `YYYY-MM-01` |
| `workMinutesTotal` | `work_minutes_total` | MonthSummary |
| `adjustMinutesBalance` | `adjust_minutes_balance` | MonthSummary |

- PunchRecord（打卡记录）
  - `id`：唯一标识
  - `userId`：用户标识（云端存储字段；同步时由顶层 `userId` 绑定，子对象不传）
  - `localDate`：归属日期（`YYYY-MM-DD`，按 `timezoneId` 的本地日期口径）
  - `type`：`START`/`END`
  - `atUtc`：打卡 UTC 时间（RFC3339，不含毫秒；分钟精度，秒/毫秒为 0）
  - `timezoneId`：打卡时区 ID（IANA 时区名，如 `Asia/Shanghai`）
  - `minuteOfDay`：本地当天第几分钟（`0..1439`）
  - `source`：`AUTO`/`MANUAL`/`MAKEUP`/`EDIT`
  - `deletedAtUtc`：（可选）删除时间（UTC，用于软删除同步）
  - `version`：版本号（用于幂等同步，MVP 口径为必填）
  - 一致性校验（同步/入库强约束）：`localDate == date(atUtc AT TIME ZONE timezoneId)` 且 `minuteOfDay == hour(atUtc@timezoneId) * 60 + minute(atUtc@timezoneId)`
- LeaveRecord（请假原始记录）
  - `id`：唯一标识
  - `userId`：用户标识（云端存储字段；同步时由顶层 `userId` 绑定，子对象不传）
  - `localDate`：归属日期（`YYYY-MM-DD`，按手机本地时区）
  - `leaveType`：`AM`/`PM`/`FULL_DAY`
  - `deletedAtUtc`：（可选）删除时间（UTC，用于软删除同步）
  - `version`：版本号（用于幂等同步，MVP 口径为必填）
  - 唯一性约束（云端）：同一 `localDate` 最多 1 条未删除请假记录（重复写入按“修改”覆盖）
- DaySummary（每日汇总，供展示与 Web 端读取）
  - `id`：唯一标识
  - `userId`：用户标识（云端存储字段；同步时由顶层 `userId` 绑定，子对象不传）
  - `localDate`：归属日期（`YYYY-MM-DD`）
  - `startAtUtc`/`endAtUtc`：（可选）当日上班/下班打卡 UTC 时间
  - `isLeaveDay`/`leaveType`
  - `isLate`：（可选）是否迟到
  - `workMinutes`：（可选）当日工时（分钟）；不可计算则为空
  - `adjustMinutes`：（可选）当日调休变动（分钟，正负）；非请假日不可计算时为空；请假日固定为 `0`
  - `status`：`INCOMPLETE`/`COMPUTED`
  - `version`：版本号（用于幂等同步，MVP 口径为必填）
  - `updatedAtUtc`：更新时间（UTC RFC3339，不含毫秒）
  - 状态约束（对齐 API/DB，便于 Web 端只读展示）：
    - `status=INCOMPLETE`：`workMinutes` 为空；非请假日 `adjustMinutes` 为空；请假日 `adjustMinutes=0`
    - `status=COMPUTED`：`workMinutes` 与 `adjustMinutes` 均不为空；`adjustMinutes` 必须为 30 分钟步进；请假日 `adjustMinutes=0`
- MonthSummary（月度汇总，供展示与 Web 端读取）
  - `id`：唯一标识
  - `userId`：用户标识（云端存储字段；同步时由顶层 `userId` 绑定，子对象不传）
  - `monthStart`：归属月份起始日期（`YYYY-MM-01`，按手机本地时区；云端主键/查询键）
  - `yearMonth`：展示派生字段（`YYYY-MM`，由 `monthStart` 格式化得到；不作为云端主键）
  - `workMinutesTotal`：月总工时（分钟）
  - `adjustMinutesBalance`：月调休余额（分钟，正负）
  - `version`：版本号（用于幂等同步，MVP 口径为必填）
  - `updatedAtUtc`：更新时间（UTC RFC3339，不含毫秒）
- User（用户/设备，建议）
  - `userId`
  - `pairingCode`
  - `pairingCodeUpdatedAt`
  - `recoveryCodeHash`
  - `writerDeviceId`
  - `writerEpoch`

## 6. 业务规则与数据口径

### 6.1 时间口径与基础定义

- 标准日工时：9 小时（= 540 分钟）。
- 正常上班时间：09:00-18:00（用于“迟到”判断与默认配置参考，不限制弹性打卡）。
- 迟到规则：上班打卡时间 **晚于 09:10** 标记为迟到（09:10 含当作未迟到）；仅在“非请假日且非周末（周六/周日）”时判定。
- 弹性工作制：只要当日实际工时达到 9 小时即可视为达标，不限制具体上下班时间。
- 午休/休息扣除：不扣除（工时按下班-上班计算）。
- 时区与日期：以打卡当时手机的本地时区确定 `localDate`；出差跨时区按当地时区记录与统计。
- 周末与节假日：MVP 不识别法定节假日/调休日历；周末仅按周六/周日（基于 `localDate`）。
- 时间精度：打卡时间以分钟为最小粒度存储与计算；秒/毫秒必须为 0（分钟精度）。
- 时间段边界：所有“时间段”均采用半开区间 `[start, end)`，按分钟比较（等价于 `start <= now < end`）。

### 6.2 每日工时计算

- 当日工时（分钟）= 下班打卡时间 - 上班打卡时间。
- 当日没有成对打卡（缺上班或缺下班）时：不计算当日工时；当日也不参与当月工时与调休/补班累计。

### 6.3 调休/补班计算（非请假日）

> 计量单位：30 分钟（半小时）。

- 设 `workMinutes` 为当日工时（分钟），`requiredMinutes = 540`。
- 差值：`delta = workMinutes - requiredMinutes`
  - 若 `delta > 0`（超出）：当日调休变动 = `floor(delta / 30) * 30`（分钟）
  - 若 `delta < 0`（不足）：当日调休变动 = `-ceil(abs(delta) / 30) * 30`（分钟）
  - 若 `delta = 0`：当日调休变动 = 0

示例（与需求口径一致）：

- 09:11 上班，18:31 下班：超出 20 分钟 → 不累计调休
- 09:11 上班，18:41 下班：超出 30 分钟 → +30 分钟调休
- 09:11 上班，19:01 下班：超出 50 分钟 → +30 分钟调休
- 09:11 上班，19:11 下班：超出 60 分钟 → +60 分钟调休
- 09:11 上班，18:01 下班：少 10 分钟 → -30 分钟调休
- 09:11 上班，17:41 下班：少 30 分钟 → -30 分钟调休
- 09:11 上班，17:31 下班：少 40 分钟 → -60 分钟调休

### 6.4 请假规则

- 请假类型：
  - 上午半天：09:00-12:00
  - 下午半天：12:00-18:00
  - 全天
- 请假当天：无论当日工作多久，均**不累计**调休，也**不扣减**调休（当日调休变动固定为 0）。
- 请假当天：若当日上下班成对打卡，仍可展示“当日实际工时”（下班-上班）；但不影响当月调休/补班余额。

### 6.5 月度统计与展示规则

- 月总工时：当月每日工时累加（仅累加可计算的 `workMinutes`；未成对打卡的不计入；展示见 6.9）。
- 当月调休余额：按“选中月份”范围内的每日“调休变动”累加，各月份独立计算；不与其他月份结转/抵扣。
- 请假日：`adjustMinutes` 固定为 0；月总工时若可计算则照常计入。
- 统计日期口径：不区分工作日/周末/节假日；只要当日可计算工时（上下班成对）就纳入当月统计。
- 展示口径：
  - 当月调休余额 ≥ 0：展示为“月可调休时长 = 余额”
  - 当月调休余额 < 0：展示为“月需补班时长 = abs(余额)”

### 6.6 自动打卡规则（App 启动触发）

- 自动打卡触发条件：App 进入前台（含冷启动、从后台回前台）时，若当前时间（分钟粒度）落在配置的时间段 `[start, end)` 内则触发。
- 全天请假：若当日请假类型为“全天”，禁用自动打卡（不生成 `START/END`）；但允许手动打卡/补卡/修改/删除。
- 上班自动打卡：
  - 当前时间在“上班时间段”内，且当日尚无上班打卡 → 生成一条 `START` 打卡（`source=AUTO`）。
  - 当日已存在上班打卡 → 不再重复生成。
- 下班自动打卡：
  - 当前时间在“下班时间段”内，且当日已存在上班打卡、但尚无下班打卡 → 生成一条 `END` 打卡（`source=AUTO`）。
  - 当日缺少上班打卡 → 禁止生成下班打卡（与 FR-008 一致）。
  - 当日已存在下班打卡 → 不再重复生成。
- 配置校验（强约束）：
  - 上班时间段与下班时间段不得重叠（允许端点相接）。
  - 单个时间段必须满足 `start < end`，禁止跨午夜。
  - 若校验失败，必须阻止保存并提示修正。

### 6.7 手动打卡 / 补卡 / 修改

- 手动打卡：在任意时间可手动生成 `START` 或 `END` 打卡（受“缺上班禁止下班”约束）。
- 补卡：选择日期与时间补录 `START`/`END`（受“每天各 1 次”约束；若已存在同类型打卡，需弹窗确认后按“修改”覆盖）。
- 修改：允许修改已存在的上班/下班打卡时间；修改后需触发重算并同步。
- 删除：允许删除已存在的上班/下班打卡记录；删除后需触发重算并同步。
- 唯一性约束：同一 `localDate` 下，`START` 最多 1 条、`END` 最多 1 条（重复写入按“修改”或提示处理）。
- 冲突处理：当同一天已存在同类型打卡记录时，再次手动打卡/补卡必须弹窗二次确认；确认后按“修改”覆盖原记录。
- 合法性校验：
  - 若同时存在 `START` 与 `END`，要求 `END > START`，否则视为无效并阻止保存。
- 跨日限制：不允许跨日打卡；同一 `localDate` 的 `START/END` 需都落在该 `localDate` 内，且 `END` 必须晚于 `START`。

### 6.8 同步与一致性口径

- 移动端为计算与写入的唯一端；Web 端只读展示（不修改）。
- 同步触发：
  - 每次生成/修改/删除一条打卡或请假记录后，立即尝试同步到云端。
  - 提供手动“同步”用于重试失败或补发。
- 同步内容：
  - 原始打卡记录（PunchRecord）
  - 请假原始记录（LeaveRecord）
  - 每日汇总（DaySummary）：在当日上下班成对后生成/更新；请假日也可生成汇总（`isLeaveDay=true`，`adjustMinutes=0`）
  - 月度汇总（MonthSummary）：由移动端按月重算并同步（供 Web 端读取展示月总工时与月可调休/需补班）

#### 6.8.1 幂等、版本、写入端与原子性（对齐 T001）

- 载荷白名单：同步请求 JSON 采用严格白名单，出现任意未知字段直接拒绝（`error_code=UNKNOWN_FIELD`）。
- 幂等（提交级）：`syncId + payloadHash` 作为幂等键：重放为 `NOOP`（`gate_reason=REPLAY_NOOP`），冲突拒绝（`error_code=SYNC_ID_CONFLICT`，`gate_reason=SYNC_ID_CONFLICT`）。
- 幂等（实体级）：实体按 `id` Upsert；当上报 `version` 低于或等于现存版本时，门控为 `NOOP`（`gate_reason=LOW_OR_EQUAL_VERSION`）。
- 写入端门控：`deviceId` 与 `writerEpoch` 必须匹配当前写入端，否则拒绝（`error_code=STALE_WRITER_REJECTED`，DB `error_key=SYNC_COMMIT_STALE_WRITER`）。
- 原子性：一次同步内 Punch/Leave/Day/Month + `sync_commit` 元数据必须同事务提交，任一失败整体回滚，避免 Web 与移动端短暂口径不一致。
- 统计口径与同步时机：
  - 当日上下班未成对时：`workMinutes` 为空；非请假日 `adjustMinutes` 为空；请假日 `adjustMinutes` 固定为 0；`status=INCOMPLETE`。移动端与 Web 端显示“未完成（缺上班/缺下班）”，并仅展示已存在的打卡时间。
  - 当日上下班成对且非请假日 → 计算并更新 `workMinutes/adjustMinutes`，并更新当月统计展示。
  - 当日上下班成对且为请假日 → 计算并更新 `workMinutes`（用于展示），`adjustMinutes` 固定为 0。
- Web 端鉴权（MVP）：Web 端通过输入配对码绑定只读查看权限；配对码可在移动端重置，重置后旧配对码失效。
- 配对码规格（MVP）：8 位数字；长期有效（直到手动重置），无过期机制。
- 多设备（MVP）：仅允许 1 台手机端作为写入端；Web 端只读查看。
- 换机迁移（MVP）：新手机通过配对码发起迁移请求；旧手机需确认；确认后旧手机强制退出且不可继续写入，新手机成为唯一写入端。
- 强制迁移（MVP）：若旧手机丢失/损坏/无法联网确认，新手机使用“配对码 + 恢复码”强制接管写入；接管后旧手机若再次上线需强制退出且不可继续写入。
- 恢复码（MVP）：16 位字母数字混合（A-Z、0-9，不区分大小写）；首次生成或重置后仅展示一次；云端仅保存哈希（不保存明文）；若遗失需重置生成新码（旧码失效）。

### 6.9 时长展示口径

- 调休/补班：以 0.5 小时步进展示（30 分钟 = 0.5 小时）。
- 工时：以“X 小时 Y 分钟”展示（Y 为 0-59）。
- 月总工时：同工时口径展示（小时+分钟）。

## 7. 约束与假设（Constraints & Assumptions）

- 一切计算在移动端 App 内完成；Web 端仅展示云端数据，不参与计算。
- 数据以移动端本地存储为主；同步到云数据库用于跨端查看与备份。
- MVP 账号体系：移动端生成配对码，Web 端输入配对码获得只读查看权限（不做扫码）。
- MVP 平台范围：仅 Android。
- 云数据库：`PostgreSQL 18.1`（Docker 部署）。
- 鉴权方式（MVP）：不引入独立账号体系；Web 端通过配对码绑定只读查看，强制迁移通过“配对码 + 恢复码”完成接管。

## 8. 验收标准（Acceptance Criteria）

> 说明：括注中标注可自动化验证的 `error_code` / `gate_reason` / DB `error_key`；同步门控与错误码映射以 `docs/同步上报实现基线-T001.md` 为准。

### 8.1 功能（Happy Path）

- [ ] 自动打卡：在上班时间段内启动 App 会生成上班打卡；在下班时间段内启动 App 会生成下班打卡（同一天不重复生成）。
- [ ] 全天请假禁用自动打卡：当日请假类型为“全天”时，App 进入前台不会生成自动打卡记录。
- [ ] 迟到判断：仅在非请假日且非周末（周六/周日）时，上班打卡晚于 09:10 标记迟到；请假日与周末不判迟到（周末仍统计工时，若上下班成对）。
- [ ] 工时与调休计算：上下班成对且非请假日时，按 9 小时与 30 分钟步进规则计算当日调休变动与月度余额（覆盖文档示例）。
- [ ] 请假日展示：请假当天调休变动始终为 0；若上下班成对打卡可展示“当日实际工时”，但不影响当月调休/补班余额。
- [ ] 月度展示：当月余额为正/0 显示可调休；为负显示需补班；调休/补班按 0.5 小时步进展示。
- [ ] 同步后可在 Web 端查看：Web 端展示与手机端一致的统计结果（以最后一次同步为准）。
- [ ] 冲突需确认：同一天已存在同类型打卡记录时，再次打卡/补卡会弹窗确认；确认后按“修改”覆盖原记录。

### 8.2 异常与负向（Negative Cases）

- [ ] END 缺 START：同一 `localDate` 存在 `END` 但无 `START` 必须拒绝。（`error_code=PUNCH_END_REQUIRES_START`，DB `error_key=PUNCH_END_REQUIRES_START`）
- [ ] END 不晚于 START：同日 `END <= START` 必须拒绝。（`error_code=PUNCH_END_NOT_AFTER_START`，DB `error_key=PUNCH_END_NOT_AFTER_START`）
- [ ] 时间精度非法：`at_utc` 非分钟粒度（秒/毫秒非 0）必须拒绝。（`error_code=TIME_PRECISION_INVALID`）
- [ ] 时间字段不一致：`local_date` 与 `timezone_id/at_utc` 不一致，或 `minute_of_day` 与 `at_utc@timezone_id` 不一致必须拒绝。（`error_code=TIME_FIELDS_MISMATCH`）
- [ ] 未知字段：同步请求出现任意未知字段必须拒绝。（`error_code=UNKNOWN_FIELD`）
- [ ] 全天请假与自动打卡冲突：`FULL_DAY` 请假日不允许出现 `source=AUTO` 的打卡。（`error_code=CONFLICT_AUTO_PUNCH_FULL_DAY_LEAVE`，DB `error_key=AUTO_PUNCH_ON_FULL_DAY_LEAVE/FULL_DAY_LEAVE_WITH_AUTO_PUNCH`）
- [ ] 自动打卡配置非法：上/下班时间段重叠、跨午夜或 `start>=end` 必须阻止保存并提示修正。（客户端校验）

### 8.3 幂等 / 版本 / 写入端（Sync Gate）

- [ ] `sync_id` 重放：相同 `sync_id` + 相同 `payload_hash` 返回 `NOOP`。（`gate_reason=REPLAY_NOOP`）
- [ ] `sync_id` 冲突：相同 `sync_id` + 不同 `payload_hash` 冲突拒绝。（`error_code=SYNC_ID_CONFLICT`，`gate_reason=SYNC_ID_CONFLICT`）
- [ ] 低/同版本：新 `sync_id` 但实体 `version` 低于或等于现存版本返回 `NOOP`。（`gate_reason=LOW_OR_EQUAL_VERSION`）
- [ ] 非写入端提交：`device_id` 或 `writer_epoch` 不匹配当前写入端必须拒绝。（`error_code=STALE_WRITER_REJECTED`，DB `error_key=SYNC_COMMIT_STALE_WRITER`）
- [ ] 原子性：一次 `sync/commits` 中 Punch/Leave/Day/Month + `sync_commit` 元数据必须同事务提交，任一失败整体回滚。

### 8.4 迁移与接管（MVP）

- [ ] 换机迁移：新手机发起迁移请求后，旧手机确认；确认后旧手机强制退出，新手机成为唯一写入端并可继续记录。
- [ ] 强制迁移：旧机不可用时，新手机使用“配对码 + 恢复码”可接管写入；旧机后续上线会被强制退出。

### 8.5 限流（MVP）

- [ ] 配对码绑定、恢复码校验、迁移申请/确认、配对码重置等接口超过限流阈值时必须拒绝并返回错误码。（`error_code=RATE_LIMIT_BLOCKED`）

### 8.6 Web 只读边界

- [ ] Web 端仅可读取云端汇总数据（DaySummary/MonthSummary）用于展示，不具备任何写入能力（无打卡、无计算、无同步写入）。
- [ ] 重置配对码后旧配对码不可再访问（绑定失效）。

## 9. 里程碑（Milestones）

- M0：需求对齐（本文档完成基础信息）
- M1：MVP 范围冻结
- M2：MVP 交付与验收

## 10. 未决问题（Open Questions）

> 下列问题需要 human 统一拍板，本文档不在此处下结论。

1. 半天请假（`AM/PM`）对自动打卡的影响：是否需要在对应半天禁用自动打卡（例如 `AM` 请假禁 `START` 自动打卡、`PM` 请假禁 `END` 自动打卡）？
2. 半天请假（`AM/PM`）对迟到判断的影响：是否仍按“请假日不判迟到”处理，还是仅 `AM` 请假时不判迟到？
3. 跨时区成对打卡口径：`START/END` 若发生在不同 `timezoneId`，是否允许组成“同一日的成对打卡”？`localDate` 以何时区为准？
4. Web 展示未完成日口径：Web 是否必须依赖 `DaySummary.status`（`INCOMPLETE/COMPUTED`）作为唯一来源，还是允许 Web 读取 PunchRecord/LeaveRecord 推导“缺上班/缺下班/请假”状态？
5. 普通迁移发起流程与入口：FR-023 的“新机发起、旧机确认”在产品交互与 API 端点上的最终落点是什么（当前 API/DB 草案以 migration request/confirm 为主）？
6. 配对码/恢复码生成端口径：由客户端生成后上报，还是由服务端生成并返回？（影响限流、暴力猜测防护与恢复策略）
7. `DeviceAuth` 是否纳入 MVP：API 草案中 `sync/commits` 标注“MVP 不校验 `DeviceAuth`”，是否接受匿名 + 写入端门控的最终口径？

## 11. 变更记录（Changelog）

- 2026-03-04：对齐 5.5 数据结构与同步白名单，明确时间段边界与配置约束，补齐同步幂等/版本/写入端/原子性，并重构验收标准分组与负例；新增未决问题清单
- 2026-02-06：初始化文档结构
- 2026-02-06：补充核心需求与业务规则（打卡/工时/调休/请假/同步/Web 只读展示）
- 2026-02-06：确认关键口径（不扣午休、默认自动打卡时间段、缺上班禁止下班、逐条同步、统计需成对打卡）并补充手动打卡/补卡/修改
- 2026-02-06：补充信息架构与数据结构建议，明确自动/手动打卡与同步口径，补充验收点并更新未决问题
- 2026-02-06：确认配对码只读查看、无审计历史、单日单对打卡、禁止跨日、按有打卡即统计、展示格式口径
- 2026-02-06：确认配对码规格（8 位数字、无过期）、前台触发自动打卡、MVP 单写入设备与换机迁移、MVP 仅 Android
- 2026-02-06：确认换机迁移需旧机确认且强制退出，配对码默认掩码并支持一键复制，Web 移动端适配非必需（可选）
- 2026-02-06：补充配对码/恢复码/迁移相关接口需要限流与失败次数限制（安全基线）
- 2026-02-06：确认旧机不可用时支持强制迁移（配对码+恢复码），并补充恢复码交互与安全口径
- 2026-02-06：补齐请假日成对打卡的同步/展示口径与验收项
- 2026-02-06：确认迟到例外（请假/周末不判迟到）、全天请假禁用自动打卡、分钟精度、冲突二次确认覆盖与删除打卡
- 2026-02-06：补齐月度汇总（MonthSummary）同步口径，明确 Web 端不做计算仅展示
- 2026-02-06：恢复码规格调整为字母数字混合
- 2026-02-06：修正验收项“逐条同步”口径，纳入删除操作
- 2026-02-06：确认性能指标为 2 秒内响应（不计网络延迟），并明确 MVP 不引入独立账号体系
- 2026-02-06：将时间段重叠校验、打卡合法性校验、同步内容从建议收敛为强约束
- 2026-02-07：同步已确认口径，移除“云数据库/鉴权方式待定”表述并与数据库方案文档对齐
- 2026-02-07：补齐同步载荷口径，明确 `LeaveRecord` 与 Punch/汇总同属同步内容
- 2026-02-07：同步触发口径扩展到请假记录，保持与 `LeaveRecord` 同步载荷一致
- 2026-02-07：明确请假能力边界为“登记/修改/删除”，消除与同步触发口径歧义
- 2026-02-07：收敛 5.5 数据结构口径（`version/timezoneId/minuteOfDay` 必填；请假日 `adjustMinutes=0`）
